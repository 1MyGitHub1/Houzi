<ltCtl:CustomWindow 
    x:Class="Monster.AutoSampler.Views.ShellPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cal="http://www.caliburnproject.org"
    xmlns:ltCtl="clr-namespace:LabTech.UITheme;assembly=LabTech.UITheme"
    TitleBarIcon="/LabMonster;component/Images/Monster24.png"
    Title="LabMonster" WindowStartupLocation="CenterScreen" 
    cal:Message.Attach="[Event Closing]=[Action MainClosing($eventArgs)]"
    IsCloseWindow="{Binding IsCloseWin,Mode=OneWay}"
    Style="{DynamicResource CustomWindowMainSty}"
    WindowState="Maximized">

    <Viewbox Stretch="Fill" StretchDirection="Both">
        <Border Width="1920" Height="1032" Background="{StaticResource CommonWhiteBrush}" BorderThickness="0">
            <!--<Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <ContentControl Grid.Column="1" cal:View.Model="{Binding ActiveItem}"/>
            </Grid>-->
            <ContentControl cal:View.Model="{Binding ActiveItem}"/>
        </Border>
    </Viewbox>
</ltCtl:CustomWindow>